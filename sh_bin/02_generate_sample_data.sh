##!/usr/bin/env bash
#set -euo pipefail
#
## 스크립트 실행 위치와 상관없이 프로젝트 루트를 찾습니다.
#SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
#ROOT_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"
#
## JSON 샘플 데이터 파일을 생성/갱신합니다.
#cd "${ROOT_DIR}"
#python3 "${SCRIPT_DIR}/generate_food_sample_data.py"

# 이건 안씀.