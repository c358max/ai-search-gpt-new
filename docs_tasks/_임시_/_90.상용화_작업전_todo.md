› 전체 프로젝트를 검토해서 이후 상용화 작업을 진행하기에 앞서 개선할 부분이 있는지 리스트업 해줘.

[Holding]
- High: 동의어 reload API가 인증/인가 없이 외부에 열려 있습니다. SearchController.java#L82 에서 /api/search/reload-synonyms를 그대로 노출하고 있는데, 프로젝트에는 Spring Security 의존성이나 보안 설정이 없습니다. build.gradle#L27 상용 전에는 최소한
관리자 인증, 내부망 제한, 감사 로그가 필요합니다.
- High: 애플리케이션 기본 설정이 개발 편의 중심이라 운영 오작동 여지가 큽니다. application.yml#L6 의 기본 ES URL이 localhost, 비밀번호 기본값이 password, auto-port-forward 기본값이 true입니다. 여기에 ElasticsearchAutoConnector.java#L56 와
  K8sPortForwarder.java#L31 가 런타임에 kubectl과 secret read를 시도합니다. 상용 앱 프로세스가 클러스터 자격증명과 port-forward 로직을 내장하는 건 분리하는 편이 맞습니다.
- High: 현재 ES 매니페스트는 개발용으로는 괜찮지만 상용 배포물로는 부적합합니다. es-cluster-custom-image.yaml#L17 은 맥북 절대경로 hostPath에 묶여 있고, es-cluster-custom-image.yaml#L42 는 TLS를 끄고, es-cluster-custom-image.yaml#L48 는 단일 노드입
  니다. 상용화 전에 RWX 스토리지/NAS, TLS, 최소 다중 노드, 리소스/스토리지 클래스 분리가 필요합니다.
- Medium: 사용자 사전 런타임 데이터가 Git에 추적되고 있어 운영 상태가 저장소를 오염시킵니다. .gitignore#L1 에 .local-nas 제외가 없고, 실제로 .local-nas/userdict/*가 추적 중입니다. git ls-files 결과 기준. 상용 전에는 런타임 데이터, 테스트 fixture, 운
  영 active 파일을 분리해야 합니다.
- Medium: 현재 색인 파이프라인은 상용 데이터 규모에 바로 확장되지 않습니다. FoodDataLoader.java#L22 가 전체 JSON을 한 번에 메모리로 읽고, ProductIndexingService.java#L37 가 전 건 임베딩을 동기 생성 후 리스트로 모아서 bulk 요청합니다.
  DjlEmbeddingService.java#L122 도 단일 predictor 호출 기반입니다. 상용 전에는 배치 스트리밍, chunking, 재시도, 병렬 임베딩, backpressure 설계가 필요합니다.

- Medium: 통합 테스트가 외부 환경에 강하게 결합돼 CI 재현성이 낮습니다. TruststoreTestBase.java#L5 는 로컬 홈 디렉터리 truststore를 강제하고, SynonymsRestClientIntegrationTest.java#L36 는 실제 색인/동의어 변경을 수행합니다. 상용화 전에는 Testcontai
  ners 또는 별도 ephemeral ES 환경으로 분리하는 편이 안전합니다.

  - 테스트용 Elasticsearch를 Testcontainers 같은 ephemeral 환경으로 띄우기
  - TruststoreTestBase의 로컬 홈 디렉터리 의존 제거
  - 테스트 프로필에서 ai-search.k8s.auto-port-forward=false 로 고정
  - 외부 클러스터를 직접 변경하는 테스트와 순수 애플리케이션 통합 테스트를 분리
  - 동의어/인덱스 롤아웃 테스트도 가능하면 테스트 전용 ES 인스턴스에서만 돌리기

- es-cluster-custom-image.yaml은 개발용 매니페스트로 보입니다. 그렇더라도 현재 저장소에 “상용화 후보” 문서들이 함께 있어서 dev/prod 산출물 경계는 더 명확히 나눌 필요가 있습니다.
- /api/search/reload-synonyms가 내부 운영용 엔드포인트일 수는 있습니다. 그래도 현재 코드상 보호 장치가 없어서 상용 진입 전엔 반드시 제한이 필요합니다.
1. 보안: 검색/운영 API 분리, 관리자 인증, secret/port-forward 로직을 앱 밖으로 이동.
3. 배포: dev 매니페스트와 prod 매니페스트 분리, TLS/RWX/NAS/다중 노드 설계.
4. 데이터/운영: .local-nas와 샘플 fixture를 Git 추적에서 분리.
5. 확장성: 색인 소스/임베딩/벌크 파이프라인을 배치형으로 재구성.
6. 테스트: 외부 의존 통합 테스트를 격리하고 CI 친화적으로 전환.
