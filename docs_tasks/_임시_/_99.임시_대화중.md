2. Medium: in-flight dedup 이름과 달리 실제 임베딩 작업은 중복 실행될 수 있습니다.
   QueryEmbeddingService.java:69, QueryEmbeddingService.java:77, QueryEmbeddingService.java:104
   startEmbedding()을 먼저 호출하고 그 다음 putIfAbsent()를 하기 때문에, 같은 검색어가 동시에 들어오면 서로 다른 future가 실제로 둘 다 시작될 수 있습니다.
   지금 구조는 “결과 합류”는 하지만 “계산 1회 보장”은 아닙니다. 성능 최적화 수준의 문제라 급하진 않지만, 이름과 보장이 어긋나서 유지보수자가 오해하기 쉽습
   니다. 최소한 주석이나 메서드명에서 보장 수준을 낮춰 표현하는 게 좋고, 정말 dedup이 필요하면 placeholder future 패턴으로 가야 합니다.
3. Medium: 카테고리 부스팅 룰 조회가 완전 일치 매칭이라 확장성이 낮습니다.
   CategoryBoostingDecider.java:32, JsonCategoryBoostRules.java:97
   findByKeyword(query)가 사실상 raw query exact match입니다. 지금은 "사과"처럼 짧은 키워드에만 맞고, "국산 사과", "사과 추천" 같은 실제 검색어로 확장되면
   룰이 바로 죽습니다. 기능 요구가 아직 단순하면 괜찮지만, “카테고리 부스팅이 잘 안 먹는다”는 이슈가 생기기 쉬운 구조입니다. 최소한 query normalization/토
   큰 단위 매칭/contains 기준 중 하나로 확장 포인트를 남겨두는 게 좋습니다.
4. Medium: 예외 분류는 좋아졌지만 IllegalStateException이 너무 넓게 500으로 묶여 있습니다.
   ApiExceptionHandler.java:39, IndexRestoreService.java:70, IndexCleanupService.java:98
   현재는 IllegalStateException이 모두 INTERNAL_SERVER_ERROR로 떨어집니다. 이 자체는 일관성 면에선 나쁘지 않지만, 내부적으로는 “설정 누락”, “복구 검증 실
   패”, “카테고리 룰 파일 문제”, “롤아웃 후처리 실패”가 같은 등급으로 뭉칩니다. 운영 로그만 봐도 원인 분류가 어렵고, API 응답 code도 너무 거칩니다. 최근 예
   외 정리를 이미 하셨으니, 다음 단계는 IllegalStateException을 던지는 주요 축만 도메인 예외로 한번 더 나누는 게 좋습니다.
5. Low: category boost 저장소 쪽에 과거 구조 흔적이 아직 남아 있어 읽는 비용이 큽니다.
   JsonCategoryBoostRules.java:20, JsonCategoryBoostRules.java:59, FileCategoryBoostRuleSource.java:69
   구조 분리는 해두셨지만, 클래스명은 아직 JsonCategoryBoostRules이고 내부엔 테스트용 경로 전환, source 분기, TTL gate, cache entry가 같이 남아 있습니다.
   기능상 문제는 없지만 처음 읽는 사람은 “왜 source로 뺐는데 이름은 Json이지?”에서 다시 멈춥니다. 지금 당장 리팩토링까지는 아니더라도, 이름을
   CachingCategoryBoostRules 쪽으로 바꾸거나 테스트용 path switching을 support 계층으로 빼면 이해 비용이 확 줄어듭니다.
