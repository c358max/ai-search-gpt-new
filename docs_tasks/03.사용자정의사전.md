# 08. 사용자 정의 사전 처리 가이드 (파일 경로 방식)

## 0. 목표
- 기존 색인 구조를 크게 흔들지 않고 `user_dict_ko.txt`를 Elasticsearch Nori tokenizer에 반영한다.
- 운영 절차는 단순하게 유지한다.
- 코드 변경은 SRP/OCP/DIP를 지키는 범위로 최소화한다.
- 현재 작업 기준으로는 ConfigMap 방식이 아니라 PV/PVC 기반 외부 파일 마운트 방식을 사용한다.

## 1. 현재 상태 요약
- 운영 active 사용자 사전 파일은 외부 마운트 경로에 존재한다.
  - `.local-nas/userdict/user_dict_ko.txt`
- 인덱스 롤아웃은 이미 구현되어 있다.
  - `src/main/java/com/example/aisearch/service/indexing/orchestration/IndexRolloutService.java`
- 현재 반영 대상:
  - `index-mapping.json`의 `user_dictionary` 경로를 `userdict/user_dict_ko.txt`로 유지
  - ECK YAML에 userdict PV/PVC 디렉터리 마운트 적용

## 2. 채택 방식
`nori_tokenizer.user_dictionary`에 ES 컨테이너 내부 파일 경로를 지정한다.

예:
```json
"tokenizer": {
  "ko_nori_userdict_tokenizer": {
    "type": "nori_tokenizer",
    "decompound_mode": "mixed",
    "user_dictionary": "userdict/user_dict_ko.txt"
  }
}
```

핵심 이유:
- 사전이 커져도 안정적
- 사전 파일을 운영에서 독립적으로 관리 가능
- analyzer 설정과 사전 원본 파일의 책임 분리가 명확함

## 3. 최소 변경 작업 순서 (권장)
1. `.local-nas/userdict/user_dict_ko.txt` 수정
2. PV/PVC 기반 외부 파일이 ES Pod에 마운트되도록 구성
4. `index-mapping.json` tokenizer를 `ko_nori_userdict_tokenizer`로 전환
5. 인덱스 롤아웃 실행(신규 인덱스 생성 + 재색인 + alias 전환)
6. 검색 검증

중요:
- 사용자 사전은 tokenizer 규칙이므로 변경 시 재색인이 필요하다.

## 4. 파일별 작업 가이드

### 4.1 사전 원본
- 파일: `.local-nas/userdict/user_dict_ko.txt`
- 책임: 운영 active 사전 파일 관리
- 규칙: 한 줄 1개 용어(주석 허용)

### 4.2 ES 매핑 템플릿
- 파일: `src/main/resources/es/index-mapping.json`
- 변경:
  - `analysis.tokenizer.ko_nori_userdict_tokenizer` 추가
  - `ko_mall_analyzer`, `ko_mall_search_analyzer`의 tokenizer를 위 이름으로 변경

예시:
```json
{
  "settings": {
    "analysis": {
      "tokenizer": {
        "ko_nori_userdict_tokenizer": {
          "type": "nori_tokenizer",
          "decompound_mode": "mixed",
          "user_dictionary": "userdict/user_dict_ko.txt"
        }
      },
      "analyzer": {
        "ko_mall_analyzer": {
          "type": "custom",
          "tokenizer": "ko_nori_userdict_tokenizer",
          "filter": ["lowercase", "ko_pos_filter"]
        },
        "ko_mall_search_analyzer": {
          "type": "custom",
          "tokenizer": "ko_nori_userdict_tokenizer",
          "filter": ["lowercase", "ko_synonym_filter", "ko_pos_filter"]
        }
      }
    }
  }
}
```

### 4.3 ECK 배포 YAML
- 파일:
  - `sh_bin/es-cluster-custom-image.yaml`
- 변경:
  - `PersistentVolume`, `PersistentVolumeClaim` 추가
  - `volumeMounts`에 `/usr/share/elasticsearch/config/userdict` 디렉터리 마운트

예시(ECK spec 일부):
```yaml
spec:
  nodeSets:
    - name: default
      podTemplate:
        spec:
          containers:
            - name: elasticsearch
              volumeMounts:
                - name: userdict-volume
                  mountPath: /usr/share/elasticsearch/config/userdict
          volumes:
            - name: userdict-volume
              persistentVolumeClaim:
                claimName: ai-search-userdict-pvc
```

주의:
- `config/userdict` 마운트 디렉터리에는 active 파일 위주로만 둔다.
- 테스트용 추가 파일을 둘 경우 비 ASCII 파일명은 피한다.

## 4.5 인덱스 롤아웃 실행 명령
색인만 실행 후 종료:
```bash
./gradlew bootRun \
  --args='--spring.profiles.active=indexing' \
  -Djavax.net.ssl.trustStore=/Users/justinpark/.ai-cert/djl-truststore.p12 \
  -Djavax.net.ssl.trustStorePassword=changeit
```

색인 + 웹 동시 실행:
```bash
./gradlew bootRun \
  --args='--spring.profiles.active=indexing-web' \
  -Djavax.net.ssl.trustStore=/Users/justinpark/.ai-cert/djl-truststore.p12 \
  -Djavax.net.ssl.trustStorePassword=changeit
```

## 5. OOP 원칙 적용 포인트

### 5.1 SRP
- 사전 파일 관리 책임: `.local-nas/userdict/user_dict_ko.txt`
- analyzer/매핑 책임: `index-mapping.json`
- 인덱스 교체 책임: `IndexRolloutService`
- 검색 API 책임: `SearchController`/검색 서비스 (변경하지 않음)

### 5.2 OCP
- 향후 사전 소스를 파일 외로 확장(DB/UI)해도 검색 API 시그니처는 유지한다.
- analyzer 변경은 인덱스 lifecycle 계층에서 흡수한다.

### 5.3 DIP
- 상위 정책(롤아웃 오케스트레이션)은 하위 구현(사전 파일 저장 방식)에 직접 의존하지 않게 유지한다.
- 구체 파일 파싱 로직은 필요 시 별도 컴포넌트로 분리하고, 롤아웃은 인터페이스(추상화)만 참조한다.

현재 코드 기준 변경 권장 범위:
- 변경 권장:
  - `src/main/resources/es/index-mapping.json`
  - `sh_bin/es-cluster-custom-image.yaml`
  - `.local-nas/userdict/user_dict_ko.txt`
- 변경 지양:
  - `SearchController`, `ProductSearchService` 등 검색 API 계층
  - `IndexRolloutService`의 롤아웃 순서 로직

## 6. 운영 절차 (단순 버전)
1. 사전 수정
2. ES Pod 내부 마운트 파일 확인
4. 인덱스 롤아웃 실행
5. 대표 쿼리 검증

운영 정책(현재 프로젝트):
- 현재 ECK 구성은 단일 노드(`count: 1`) 기준이다.
- 우선은 ES 재시작 없이 신규 인덱스 생성 + 재색인 + alias 전환으로 반영하는 절차를 사용한다.
- 작업은 트래픽이 낮은 시간대에 수행한다.

대표 검증 쿼리:
- `얇은피`
- `얄피`
- `교자`
- `떡국`

### 6.1 분석기(_analyze) 검증 예시
`얇은피`가 단일 토큰으로 유지되는지 확인:
```bash
curl -u elastic:${AI_SEARCH_ES_PASSWORD:-password} \
  -X POST "http://localhost:9200/food-products-read/_analyze" \
  -H "Content-Type: application/json" \
  -d '{
    "analyzer": "ko_mall_search_analyzer",
    "text": "얇은피 만두"
  }'
```

판정 기준:
- `얇은피` 토큰이 분해되지 않고 기대 형태로 유지되는지 확인

### 6.2 검색 검증 판정 기준
- `q=얇은피`, `q=얄피` 검색 시 만두 관련 상품이 포함되어야 한다.
- 기존 기능 회귀가 없어야 한다.
  - 가격 필터, 카테고리 필터, 정렬, 페이징 정상 동작

## 7. 장애 대응/롤백
1. 신규 인덱스 검증 실패 시 alias 전환을 수행하지 않는다.
2. 이미 alias 전환 후 문제 발생 시 즉시 직전 안정 인덱스로 복구한다.
3. 문제 원인(사전 용어/매핑/마운트) 수정 후 신규 인덱스로 재롤아웃한다.

alias 롤백 예시:
```bash
curl -u elastic:${AI_SEARCH_ES_PASSWORD:-password} -X POST "http://localhost:9200/_aliases" \
  -H "Content-Type: application/json" \
  -d '{
    "actions": [
      { "remove": { "index": "food-products-v20260219150000", "alias": "food-products-read" } },
      { "add":    { "index": "food-products-v20260219143000", "alias": "food-products-read" } }
    ]
  }'
```

## 8. 체크리스트
- [ ] `index-mapping.json`에서 user dictionary tokenizer가 적용되었는가
- [ ] ECK YAML에 volume/volumeMount가 반영되었는가
- [ ] PV/PVC와 `mountPath=/usr/share/elasticsearch/config/userdict`가 정상 반영되었는가
- [ ] 신규 인덱스 생성/재색인/alias 전환이 완료되었는가
- [ ] `_analyze` 결과가 기대 토큰 형태를 만족하는가
- [ ] 검색 회귀(필터/정렬/페이징) 없는가

## 9. 구축 완료 후 단어 변경/적용 방법
초기 구축(매핑 반영 + YAML 마운트 반영)이 끝난 이후에는 아래 스크립트로 반복 운영한다.

1. `.local-nas/userdict/user_dict_ko.txt` 수정
2. 적용 스크립트 실행
```bash
./sh_bin/05_1_reloadUserDictionary.sh
```
3. 검증 스크립트 실행
```bash
./sh_bin/05_2_verifyUserDictionary.sh
```

`05_1_reloadUserDictionary.sh`가 수행하는 작업:
- active 외부 사전 파일 존재 확인
- ES Pod 내부 마운트 파일 존재 확인
- `indexing` 프로파일로 재색인 + alias 전환

`05_2_verifyUserDictionary.sh`가 수행하는 작업:
- Elasticsearch `_analyze` 호출
- 기본 검증: `얇은피 만두`에서 `얇은피` 토큰 존재 확인

환경변수 오버라이드 예:
```bash
NAMESPACE=ai-search \
STATEFULSET=ai-search-es-es-default \
TRUSTSTORE_PATH=/Users/justinpark/.ai-cert/djl-truststore.p12 \
TRUSTSTORE_PASSWORD=changeit \
./sh_bin/05_1_reloadUserDictionary.sh
```

```bash
NAMESPACE=ai-search \
ANALYZE_INDEX=food-products-read \
ANALYZER_NAME=ko_mall_search_analyzer \
ANALYZE_TEXT="얄피 만두" \
ANALYZE_EXPECTED_TOKEN="얄피" \
./sh_bin/05_2_verifyUserDictionary.sh
```

운영 포인트:
- 사용자 사전 변경은 tokenizer 규칙 변경이므로 항상 재색인이 필요하다.
- 매핑 구조를 다시 바꾸는 작업은 필요 없고, "사전 파일 갱신 -> 롤아웃"만 반복하면 된다.
- 단일 노드 구성 특성상 fallback으로 ES 재시작이 필요할 때는 짧은 검색 장애 가능성을 고려해야 한다.
- 단, 최초 1회는 `00_6_start_elasticsearch_cluster_custom_image.sh`로 userdict PV/PVC 마운트 포함 CR이 적용되어 있어야 한다.

## 10. 결론
- 이 프로젝트에서는 **파일 경로 방식(`user_dictionary`)**이 가장 단순하고 유지보수성이 높다.
- 구현 포인트는 매핑 + 마운트 + 롤아웃 3가지이며, 검색 API 계층은 건드리지 않는 것이 원칙이다.
